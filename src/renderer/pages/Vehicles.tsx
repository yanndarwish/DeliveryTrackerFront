import { vehicles } from "renderer/mock"

export interface IVehiclesProps {

}

const Vehicles = (props: IVehiclesProps) => {
    console.group(vehicles)
    return (
        <div>

        </div>
    )
}

export default Vehicles